RewriteEngine On
# Remove trailing slashes
RewriteRule ^(.*)/$ /$1 [L,R=301]

# Route all requests not pointing to an actual file to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
